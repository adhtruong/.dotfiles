#!/usr/bin/env bash
set -euo pipefail

HS="/opt/homebrew/bin/hs"

app_name="${1:-}"
[[ -z "$app_name" ]] && { echo "Usage: $0 <app_name>" >&2; exit 1; }

"$HS" -c "focusApp([[$app_name]])" 2>/dev/null
